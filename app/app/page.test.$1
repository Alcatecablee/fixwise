import { render, screen } from '@testing-library/react';
import HomePage from '.\web-app\app\page.tsx';

describe('HomePage', () => {
  it('renders without crashing', () => {
    render(<HomePage />);
    expect(screen.getByText(/.+/)).toBeInTheDocument();
  });
});